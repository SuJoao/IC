To build:
	cd src; make; cd ..

To test:
	cd test
	../bin/text2bin text-bits bin-bits // converts textual binary digits into binary bits
	../bin/bin2text bin-bits text-out // converts binary bits into textual binary digits
	cmp text-bits text-out // compares the original and recovered text files; should be silent

	// exercise 6
	cd test
	../bin/wav_quant_enc <input wav file> <output bin file> <quantization bits>

	../bin/wav_quant_dec <input bin file> <output wav file>

	// exercise 7
	cd test
	../bin/wav_lossy_enc <input wav file> <output bin file> <quantization bits> <block size>
	
	../bin/wav_lossy_dec <input bin file> <output wav file>